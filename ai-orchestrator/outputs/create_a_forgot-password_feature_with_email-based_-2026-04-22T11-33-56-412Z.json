{
  "task_understanding": "Implement a forgot-password flow for LocalChat where a user submits their email to request a password reset, receives an email with a tokenized reset link, and can use that link to set a new password. Requires a request endpoint, token storage, email delivery, and a reset endpoint, plus corresponding UI screens and end-to-end validation.",
  "breakdown": {
    "frontend": "Build two screens: (1) /forgot-password — form with email input (Zod validation), calls POST /api/auth/forgot-password, shows generic success toast regardless of whether email exists (to prevent enumeration). (2) /reset-password?token=... — form with new password + confirm password fields, calls POST /api/auth/reset-password, redirects to /login on success. Add a 'Forgot password?' link on the existing login screen. Use React Hook Form + Zod, TanStack Query mutations, Tailwind styling consistent with existing auth screens.",
    "backend": "Add two endpoints under /api/auth: (1) POST /forgot-password — accepts {email}, always returns 200 with generic message, looks up user, generates a cryptographically secure token (crypto.randomBytes(32).toString('hex')), stores hashed token (sha256) + expiry (1 hour) in a new password_reset_tokens table, enqueues a BullMQ job to send email with link {APP_URL}/reset-password?token={rawToken}. (2) POST /reset-password — accepts {token, newPassword}, hashes token, looks up unused non-expired record, validates password strength (min 8 chars), bcrypt-hashes and updates user password, marks token as used, invalidates all existing refresh tokens for that user. Create migration for password_reset_tokens (id BIGINT PK, userId BIGINT FK, businessId BIGINT, tokenHash VARCHAR(64) UNIQUE, expiresAt DATETIME, usedAt DATETIME NULL, createdAt DATETIME). Add rate limiting on forgot-password (e.g., 5 per hour per IP + per email). Create email worker/template for password reset. Note: these endpoints are public (no JWT), but businessId is still recorded on the token row based on user lookup.",
    "qa": "Validate both UI flows with Playwright and API behavior with axios. Cover: successful request with valid email, request with non-existent email (should still return generic success), invalid email format (client-side validation), rate limit enforcement, valid token reset success, expired token rejection, already-used token rejection, malformed/invalid token rejection, password strength validation, confirm-password mismatch, refresh tokens invalidated after reset (old refresh token no longer works), login works with new password and fails with old password, link in email contains correct token format."
  },
  "assigned_agents": [
    "frontend",
    "backend",
    "qa"
  ],
  "missing_requirements": [
    {
      "question": "Which email delivery provider should the backend use for sending the reset email?",
      "type": "choice",
      "options": [
        "AWS SES",
        "SendGrid",
        "Resend",
        "SMTP (nodemailer with generic SMTP creds)"
      ]
    },
    {
      "question": "How long should the password reset token remain valid?",
      "type": "choice",
      "options": [
        "15 minutes",
        "1 hour",
        "24 hours"
      ]
    },
    {
      "question": "Should submitting forgot-password for a non-existent email return the same generic success response (to prevent email enumeration)?",
      "type": "yesno"
    },
    {
      "question": "Should all active sessions/refresh tokens be invalidated after a successful password reset?",
      "type": "yesno"
    }
  ],
  "agent_instructions": {
    "frontend": "1. Create app/(auth)/forgot-password/page.tsx: centered card layout matching existing login screen. Use React Hook Form + Zod schema { email: z.string().email() }. 2. Create a TanStack Query useMutation hitting POST /api/auth/forgot-password with {email}. On success (or error) show the same neutral toast: 'If an account exists for that email, a reset link has been sent.' Disable submit button while pending. 3. Add a 'Forgot password?' link on the existing login page pointing to /forgot-password. 4. Create app/(auth)/reset-password/page.tsx: reads ?token= from URL via useSearchParams. If missing, show error state with link back to /forgot-password. 5. Form fields: newPassword, confirmPassword. Zod schema: password min 8 chars, must match confirmPassword (.refine). 6. useMutation POST /api/auth/reset-password with {token, newPassword}. On 200: toast success and router.push('/login'). On 400/410: show inline error ('This reset link is invalid or has expired. Request a new one.') with link to /forgot-password. 7. Style with Tailwind using existing design tokens. Ensure both pages are in the unauthenticated layout (no JWT required). 8. Add loading/disabled states and proper aria labels for accessibility.\n\n--- User clarifications ---\nQ: Which email delivery provider should the backend use for sending the reset email?\nA: SMTP (nodemailer with generic SMTP creds)\n\nQ: How long should the password reset token remain valid?\nA: 12hours\n\nQ: Should submitting forgot-password for a non-existent email return the same generic success response (to prevent email enumeration)?\nA: No\n\nQ: Should all active sessions/refresh tokens be invalidated after a successful password reset?\nA: Yes",
    "backend": "1. Migration: create password_reset_tokens table (id BIGINT AUTO_INCREMENT PK, userId BIGINT NOT NULL, businessId BIGINT NOT NULL, tokenHash CHAR(64) NOT NULL UNIQUE, expiresAt DATETIME NOT NULL, usedAt DATETIME NULL, createdAt DATETIME DEFAULT CURRENT_TIMESTAMP, INDEX(userId), INDEX(expiresAt)). 2. Add routes in src/routes/auth.ts: POST /forgot-password, POST /reset-password — both public (no authMiddleware). 3. Forgot-password handler: Zod-validate {email}. Apply rate limit middleware: max 5 requests/hour per IP AND per email. Look up user by email scoped across all businesses. If found, generate rawToken = crypto.randomBytes(32).toString('hex'); tokenHash = sha256(rawToken). Insert row with userId, businessId (from user), tokenHash, expiresAt = now + 1h. Enqueue BullMQ job 'send-password-reset-email' with {to: user.email, name: user.name, resetUrl: `${process.env.APP_URL}/reset-password?token=${rawToken}`}. Always respond 200 {message: 'If an account exists for that email, a reset link has been sent.'} regardless of user existence (constant-time-ish). 4. Reset-password handler: Zod-validate {token: string, newPassword: string().min(8)}. Compute sha256(token). Find token row where tokenHash matches, usedAt IS NULL, expiresAt > NOW(). If not found → 400 {error:'Invalid or expired reset link'}. Load user, bcrypt.hash(newPassword, 12), UPDATE users SET passwordHash=? WHERE id=? AND businessId=?. UPDATE password_reset_tokens SET usedAt=NOW() WHERE id=?. Delete/invalidate all refresh tokens for that userId. Respond 200 {message:'Password reset successful'}. Wrap in transaction. 5. Email worker: create src/workers/passwordResetEmail.worker.ts consuming the queue, rendering an HTML+text template with the reset link and a note that it expires in 1 hour, using the chosen provider. 6. Add env vars: APP_URL, and provider credentials. 7. Log events (without logging raw tokens) via existing logger. 8. Ensure all DB queries that touch users include businessId in WHERE clause once resolved from the token/user row.\n\n--- User clarifications ---\nQ: Which email delivery provider should the backend use for sending the reset email?\nA: SMTP (nodemailer with generic SMTP creds)\n\nQ: How long should the password reset token remain valid?\nA: 12hours\n\nQ: Should submitting forgot-password for a non-existent email return the same generic success response (to prevent email enumeration)?\nA: No\n\nQ: Should all active sessions/refresh tokens be invalidated after a successful password reset?\nA: Yes",
    "qa": "Playwright UI tests: (1) From /login, click 'Forgot password?' → lands on /forgot-password. (2) Submit valid registered email → neutral success toast shown. (3) Submit unregistered email → same neutral success toast (no enumeration). (4) Submit invalid email format → client-side Zod error, no network call. (5) Visit /reset-password without token → error state with link back. (6) Visit /reset-password?token=INVALID, submit new password → inline error message. (7) Happy path: request reset → retrieve token from test mail hook/DB → visit reset link → set new password → redirected to /login → login succeeds with new password and fails with old password. (8) Password < 8 chars → validation error. (9) Confirm password mismatch → validation error. Axios API tests: (a) POST /api/auth/forgot-password with valid/invalid/missing email → always 200 with generic message for valid-format emails; 400 for invalid format. (b) Rate limit: 6th request within an hour returns 429. (c) POST /api/auth/reset-password with expired token (manually age the row) → 400. (d) With already-used token → 400. (e) With malformed token → 400. (f) Successful reset invalidates previously issued refresh tokens (old refresh cookie → 401 on /api/auth/refresh). (g) businessId scoping: password update only affects the correct user's row. (h) Token is stored hashed (query DB and assert tokenHash ≠ raw token).\n\n--- User clarifications ---\nQ: Which email delivery provider should the backend use for sending the reset email?\nA: SMTP (nodemailer with generic SMTP creds)\n\nQ: How long should the password reset token remain valid?\nA: 12hours\n\nQ: Should submitting forgot-password for a non-existent email return the same generic success response (to prevent email enumeration)?\nA: No\n\nQ: Should all active sessions/refresh tokens be invalidated after a successful password reset?\nA: Yes"
  },
  "_refinement": {
    "refined": "Implement a forgot-password feature that allows users to request a password reset by submitting their email address, then receive an email containing a reset link they can use to set a new password.",
    "notes": "Expanded the short phrase into a concrete minimum flow without adding unmentioned details.",
    "accepted": "refined"
  },
  "_clarifications": [
    {
      "question": "Which email delivery provider should the backend use for sending the reset email?",
      "answer": "SMTP (nodemailer with generic SMTP creds)"
    },
    {
      "question": "How long should the password reset token remain valid?",
      "answer": "12hours"
    },
    {
      "question": "Should submitting forgot-password for a non-existent email return the same generic success response (to prevent email enumeration)?",
      "answer": "No"
    },
    {
      "question": "Should all active sessions/refresh tokens be invalidated after a successful password reset?",
      "answer": "Yes"
    }
  ]
}